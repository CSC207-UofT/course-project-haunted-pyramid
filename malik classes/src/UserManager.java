public class UserManager {

    public boolean manageAddCourse(Course course){
        // check if there are no conflicts with other courses, alert if numberOfCourses greater then 6.
        return true;
    }
}
